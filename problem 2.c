// write a programme to calculate the area of a circle
// radius given

#include <stdio.h>
int main(){

    int radius;
    printf("enter radius");
    scanf("%d", &radius);

    printf("area of circle is %d", 3.14*radius*radius);
    
    return 0;
}